[project]
name = "local-rag"
version = "0.1.0"
description = "Local RAG app with QMD and Docker Model Runner"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.30.0",
    "requests>=2.31.0",
]

[tool.uv]
dev-dependencies = []
