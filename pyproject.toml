[project]
name = "qmd-dmr-demo"
version = "0.1.0"
description = "Local RAG app combining QMD (hybrid markdown search) with Docker Model Runner"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.30.0",
    "requests>=2.31.0",
]

[tool.uv]
dev-dependencies = []
